# Brand Monitor Dashboard

### Navodila za Render Deployment:
1. **Build Command:** `pip install -r requirements.txt`
2. **Start Command:** `python -m streamlit run app.py --server.port $PORT --server.address 0.0.0.0`
3. **Environment Variables:** Python verzija naj bo vsaj `3.9`.